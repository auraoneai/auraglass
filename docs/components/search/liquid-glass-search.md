# Liquid Glass Search

`LiquidGlassSearchField` and `LiquidGlassSearchTab` provide adaptive search placement. Use bottom placement on phones, top-trailing or centered placement on wider screens, and a search tab when search is a primary app destination.
