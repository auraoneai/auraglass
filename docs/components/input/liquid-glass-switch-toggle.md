# Liquid Glass Switch And Toggle

Modernized `GlassSwitch` and `GlassToggle` should use capsule geometry, visible focus, correct `aria-checked` semantics, disabled state clarity, and reduced-motion-safe thumb behavior.
