# Liquid Glass Action Sheet

Modernized `GlassActionSheet` behavior should support source transitions, interruptive dimming, and parallel floating presentation. Existing action sheet APIs should remain compatible.
