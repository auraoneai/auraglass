# LiquidGlassConcentricFrame

`LiquidGlassConcentricFrame` standardizes nested corner geometry. Use it when controls sit inside sheets, toolbars, sidebars, cards, or media control groups.

It exposes CSS variables for parent radius, inset, and computed child radius so children can align with the surrounding Liquid Glass shape.
